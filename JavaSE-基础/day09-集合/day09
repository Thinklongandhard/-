day09- 集合
 
   集合类的特点：提供一种存储空间可变的存储模型，存储的数据容量
   可以发生改变
   
   集合和数组的区别：
      1、共同点
	     都是存储数据的容器
	  2、不同点
	     数组的容量是固定的，集合的容量是可以改变的
		 
	集合泛型
	  ArrayList<E>
	    1、可以调整大小的数组实现
		2、<E>是一种特殊的数据类型，泛型
	空参集合的创建，初始长度为10
	
	集合中常用的方法：
	  删除
	   1、String b = arrayList.remove(index:1);  //根据索引  返回删除的元素
          boolean a = arrayList.remove(o:"1");  //根据元素 返回是否成功删除 
		  
	  修改
	   2、String c = arrayList.set(2,"aaa"); //根据索引修改当前位置的元素
	      返回被修改的元素
		  
	  查询
	   3、 String one = arrayList.get(0);  //根据索引获取元素
           String two = arrayList.get(1);
		   
	  返回集合中的元素个数
	   4、arrayList.size()；//返回当前集合的元素个数